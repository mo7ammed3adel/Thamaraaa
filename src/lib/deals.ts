/**
 * Deal statuses counted as contracted revenue for fund targets and executive
 * reporting - a signed contract counts even while installments are still
 * being collected (Pending), matching the Chief Sales overview definition.
 */
export const FUND_DEAL_STATUSES = ["Closed_Won", "Pending"];
